# === Database connection parameters
srcServer:
  - rdbms: postgresql
    host: localhost
    port: 5433
    rdbmsPath: /Applications/Postgres.app/Contents/Versions/17/bin/psql
    databaseId: uhf
    schemaId: supplier_part
    user: uhf
    pass: uhf

# === Historicization parameters
temporalModel: UHF # (BCDM, DDLM, UHF)
schemaTemporalCategory: V # (V, T, VT)
author: uhf
outDirPath: demo/Supplier-Part/res
version: 1.0
includeVxx: false
executeScript: true
useMode: physical # (virtual, physical, both)
